export enum ETableName {
  USERS = 'users',
  REFRESH_TOKENS = 'refresh_tokens',
  BRANDS= 'brands',
  MODELS='models',
  ACTION_TOKENS = 'action_tokens',
  CARS='cars',
  IMAGES = 'images',
  PRICES = 'prices',
  POSTS = 'posts',
  CURRENCIES = 'currencies'
}
